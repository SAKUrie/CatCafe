# 项目最高优先级原则

## 代码只驱动，所有数据走表格

- 代码只负责通用逻辑、流程和数据驱动，不允许在代码中硬编码业务数据。
- 所有游戏内容、数值、概率、规则、文案、资源路径、关卡参数和可配置 UI 参数都必须来自项目表格及其正式导出数据。
- 新增或修改数据时必须先修改源表格，再通过项目既有导出流程生成运行时数据；禁止用代码常量、条件分支或临时默认值绕过表格。
- 读取不到表格数据时应明确报错或使用表格体系内定义的兜底项，不允许在业务代码中静默补写一套数据。
- 除非用户明确要求修改数据结构或底层通用机制，否则不得改变表格字段含义、导出链路或数据驱动边界。

## 所有资产禁止压缩画质

- 所有图片、纹理、图标、字体、音频、视频、动画及其他资产必须保留源文件的原始尺寸、清晰度、色深、透明通道和音视频码率；禁止有损压缩、降采样、降低分辨率或使用会降低质量的 Unity 导入设置（包括 Texture Compression、Crunch Compression 和造成缩放的 Max Size），除非用户明确授权某个具体资产。

## 临时文件必须清理

- 生成的中间文件、临时文件、预览图、测试导出、构建脚本、处理脚本、缓存目录和废弃方案文件，使用完成后必须立即清理。
- 正式结果复制或写入目标位置并验证通过后，必须删除对应的临时副本和生成缓存；项目内只允许保留用户需要的最终交付文件。
- 禁止把临时文件长期留在 `Assets`、`GameDesign`、项目根目录或其他项目目录中，禁止以不同版本名堆积无用资产。
- 清理前必须核对准确路径，只删除本次任务产生且已经确认不再需要的文件；不得误删用户原有文件或其他未确认内容。
- 任务结束前必须检查本次临时目录与生成缓存是否仍然存在，并在最终回复中说明清理结果。

# 项目 UI 强制约束

- 游戏页面比例固定为 Unity Game View 的 `16:9 Aspect`。
- 所有 UI 布局、坐标、尺寸和点击区域均以固定的 16:9 画面为基准（当前参考分辨率为 1536×864）。
- 不做其他宽高比、分辨率、Safe Area、横竖屏或响应式页面适配；禁止为了适配而自动拉伸、重排或缩放 UI。
- 现有 UI 的位置、尺寸、锚点、缩放、层级、遮挡关系、点击区域和美术资源均视为已锁定，不允许随意改动。
- 只有用户明确指定某个 UI 元素需要调整时，才允许修改该元素；必须保持未被点名的 UI 完全不变，禁止顺带重构或统一调整其他 UI。
- 所有文字必须与其实际承载背景保持明显的视觉对比；禁止使用与背景颜色相近的字体颜色。UI 验收时必须逐项检查文字可读性，存在低对比度文字即视为验收不通过。
- 禁止通过新增遮挡层、色块、重复面板、裁剪贴片或相似的覆盖方式掩盖原图问题；应直接修正或清空正式源资产中的问题区域后再使用。运行时图层只能承载真实的动态 UI 内容，不能充当修图遮罩。
- 每次 UI 修改后，必须在 `16:9 Aspect` 下检查布局、遮挡和点击区域。

# Windows 打包窗口强制约束

- Windows 正式包必须以可缩放、可最大化的窗口模式运行；必须启用 Unity Player 的 `Resizable Window`，禁止交付最大化按钮不可用或窗口尺寸被锁死的包。
- Windows 运行时必须在原生窗口层拦截用户拖拽缩放（包括拖拽四边和四角），强制游戏客户区始终保持严格的 `16:9`；不得仅依赖 Canvas 缩放、黑边、延迟纠正或松手后修正来伪装比例锁定。
- 点击最大化按钮时，必须以当前显示器工作区为边界，计算其中能够容纳的最大 `16:9` 游戏客户区，并将窗口居中；禁止直接按非 `16:9` 的工作区尺寸拉伸铺满。
- 最大化、还原以及跨显示器后仍须保持 `16:9`；窗口不得超出当前显示器工作区，也不得被任务栏遮挡。
- 每次 Windows 打包后必须实际运行包体，分别验收四边拖拽、四角拖拽、最大化、还原和再次拖拽；任一步骤出现非 `16:9` 客户区、最大化按钮不可用、窗口偏离工作区或 UI 拉伸，即视为打包验收不通过。
